# Better Sicau 项目审查与优化方案

审查日期：2026-09-10。下文问题清单是修改前的审查记录；2026-09-11 已按用户要求实施主要逻辑与界面修复，见文末实施记录。

## 范围与结论

检查了 SwiftUI 页面、AppStore、领域模型与日期计算、教务抓取与 HTML 解析、认证与会话、网络与 Cookie/Keychain、验证码识别、日志、工程配置及现有测试。第三方 SwiftSoup 检查其集成和版本记录，未逐行审计第三方实现；未反编译 IPA，未使用真实账号访问学校接口，未进行真机视觉验收。

优先修复数据与界面状态的一致性，再改布局。现有服务协议、依赖注入、网络错误分类和测试基础可以保留，不需要整体重写。

P1 表示应优先修复的数据错误、漏显或会话问题；P2 表示后续修复的交互和维护问题。以下静态可确认的缺陷与需视觉验证的风险分开描述。

## P1：确定的逻辑与显示缺陷

### 1. 切学期时请求被丢弃，旧课表可能继续显示

- 位置：`test/UI/AppStore.swift:509`、`test/UI/ScheduleView.swift:56`。
- 触发：A 学期正在加载时切换 B。`loadSchedule` 因 `.schedule` 正在加载直接返回；A 的任务如果被取消，新任务也可能已被这个 guard 拦截；若 A 来自首页等独立 Task，则返回后直接写入当前数组。
- 结果：选择已变成 B，数据仍是旧学期，或加载结束后没有 B 的请求。即使没有竞态，切换期间也保留旧数组并按新学期计算周次。
- 方案：课表快照包含 accountID、term、requestID；按学期缓存。选择变化时取消旧任务并确保最新选择最终被加载；回写前验证请求身份。新学期不能显示未标注的旧数据。
- 验收：延迟 A 响应，连续 A→B→C，最终只能显示 C；包括取消、失败、缓存命中三种路径。

### 2. 登出清空不能阻止在途请求重新写入

- 位置：`test/UI/AppStore.swift:419`、`:483`；`test/Academic/LiveAcademicService.swift:193`；`test/Services/SicauSession.swift:884`。
- 触发：查询正在等待网络时退出登录。清空数组和缓存之后，旧查询恢复执行，仍可写回 AppStore 与服务缓存；网络响应也可重新加入 CookieJar。
- 结果：旧账号数据可能在换号后出现；旧请求抛出的认证失效错误也可能干扰新会话。actor 只保护同步访问，不会把跨 await 的整个业务操作变成事务。
- 方案：登出先递增 sessionGeneration，再取消账号关联任务；UI、缓存写入、Cookie 更新和错误处理均验证 generation；缓存键包含账号身份。清理逻辑集中为一个会话重置入口。
- 验收：旧账号成功/失败响应分别在新账号登录后到达，均不得更新新会话、缓存或通知。

### 3. 课表星期标题与日期错位

- 位置：`test/UI/ScheduleOverviewView.swift:305`；`test/Academic/LiveAcademicService.swift:40`。
- 证据：日期按 `startDate + (week-1)*7 + (day-1)` 生成，而 day=1 的标题固定为周一。预设 `2026-03-01` 是周日，因此周一列显示周日日期，今日高亮也错列。首页却用真实 weekday 判断课程，两处结果不一致。
- 方案：区分报到日期与第一教学周周一，明确学校周次定义，所有日期和教学周计算使用同一教学日历；业务时区统一为 Asia/Shanghai，时钟可注入。
- 验收：周日开学、周一开学、学期前后、跨年、跨时区与前后台切换。

### 4. 考试“保留未知数据”在最终筛选中失效

- 位置：`test/UI/AppStore.swift:171`、`:215`；`test/UI/ExamsView.swift:27`。
- 触发：考试 term 为空且时间待定，或选中学期缺少开学日期。`visibleExams` 虽然保留条目，最终 `examsForSelectedTerm` 仍要求 term 精确相等。
- 结果：未知学期考试被隐藏；页面的“全部”只放开考试类型，不能找回这些数据，也没有学期选择入口。
- 方案：保留上游学期；推断学期单独存储并标明来源。考试页提供可见的学期过滤、“全部学期”和“学期待确认”分组；日期推断不能静默删除记录。
- 验收：无学期、无日期、未知学期、无开学日期、跨学期补考均可在页面找回。

### 5. 课表重叠课程被遮挡，待定课程不渲染

- 位置：`test/UI/ScheduleOverviewView.swift:148`、`:225`；`test/Academic/HTMLParser.swift:661`。
- 证据：所有课程在同一个 ZStack 中按星期和节次定位，没有冲突排布；相同或相交时段相互覆盖。缺少 weekday 或 sectionStart 的待定课程进入 visibleItems 和计数，却被 courseBlock 的条件跳过。
- 方案：按当天区间冲突分组，采用并排或“同时间 N 项”聚合卡片，点击展示全部详情；待定课程单独列在网格下方。统计区分课程门数和上课安排数。
- 验收：完全重叠、部分重叠、单双周交替、只有待定课程的课表。

### 6. 部分考试抓取失败被缓存成完整成功

- 位置：`test/Academic/LiveAcademicService.swift:84`、`:289`。
- 证据：正考和缓补考任一请求成功，就绕过另一侧 terminal error 检查，将成功部分缓存五分钟；页面不知道数据不完整。另一个问题是学期确认时，把“没有选中学期且没有学期选项”视作 verified，无法证明切换成功。
- 方案：先传播认证失效和取消；普通部分失败返回携带来源状态的快照，保留已有成功数据，页面显示缺失来源和重试入口。学期确认要求正向证据，无法确认时显示查询失败，不能给数据贴上目标学期标签。
- 验收：任一来源超时/认证失败/取消，以及 HTTP 200 返回无关页面；切学期返回无选项页面。

## P2：交互与数据质量

### 7. 首页刷新实际上继续读缓存

- 位置：`test/UI/AppStore.swift:468`、`:478`。
- `refreshAll()` 调用初始加载，而三个查询都使用 forceRefresh=false；缓存期内下拉和按钮不会强制取得新数据。
- 将初始加载与用户刷新拆分，显式传递 forceRefresh=true；展示各模块最后更新时间。用 mock 记录 forceRefresh 参数验证。

### 8. 空、失败、未加载和无法判断共用空态

- 位置：`test/UI/DashboardView.swift:55`；`test/UI/AppStore.swift:483`；各查询页面。
- 首次失败只弹全局 alert，关闭后显示“暂无”；首页在开学日期缺失、查看历史学期时也显示“今天没有课程”。初始串行加载阶段尚未开始的模块会先显示 0/暂无。
- 每个模块使用 idle/loading/loaded/failed 状态，并保留旧数据、更新时间和错误信息；首次失败显示重试，刷新失败保留数据并标记。无法计算今日课表时明确提示设置教学日期或返回当前学期。
- 首页考试现在按日期升序取前三条，历史考试可长期占据摘要；如设计为近期提醒，应先区分未结束、已结束与时间待定，再排序。

### 9. 自动识别覆盖手工验证码，登录方式缺少统一生命周期

- 位置：`test/UI/AppStore.swift:279`、`:307`、`:588`；`test/UI/LoginView.swift:44`。
- OCR 返回时只检查 challengeID，用户在等待中输入的验证码仍会被覆盖。低置信度且 text 非空时，登录页也可能没有手动输入提示。
- 微信轮询不检查当前 loginMode/是否已经登录；切换到密码或短信登录时没有停止入口调用，登出也没清空 wechatState，回前台可能恢复旧轮询。
- 为自动填入增加输入版本校验，仅在用户未编辑时写入；把 OCR 状态显式化。认证任务采用互斥状态机，切模式与完成登录时取消旧流程，回调验证流程 ID。

### 10. 清除保存账号后，内存密码仍留在表单

- 位置：`test/UI/AppStore.swift:439`、`:419`。
- clearSavedCredentials 仅删除持久化凭据和改开关，未清空 username/password；登出也保留 password。用户清除后再退出，登录表单仍持有原密码。
- 清除凭据时同步清空相应内存字段；不记住密码的成功登录应释放密码字符串引用；登出按明确的记住密码策略重置表单，并清理手机号、验证码与微信状态。

### 11. 周次边界、日期输入与更新机制不完整

- 位置：`test/Core/ScheduleWeek.swift:82`、`:103`；`test/UI/ScheduleOverviewView.swift:288`；`test/UI/AppStore.swift:254`。
- parseStartDate 使用 isLenient=true，会把部分不存在的日期归一化，不能充当严格的表单验证。
- 总览初始化直接采用 currentWeek，可能超过 maximumWeek；显示历史学期会跳到没有课程的远期周，“上一周”再突然跳回最大周。
- now 是计算闭包，没有跨午夜发布事件；应用保持打开时，日期显示依赖其他状态变化才刷新。
- 日期输入用 DatePicker 或严格解析；统一有效周次范围，区分历史浏览与当前教学周。前台恢复和午夜更新业务日期。短信冷却改为发送截止时间计算，避免后台暂停导致倒计时偏慢。

### 12. 模型 ID 与业务去重口径不一致

- 位置：`test/Core/Models.swift:44`、`:90`、`:104`；`test/Academic/LiveAcademicService.swift:403`。
- ScheduleItem.id 不含 weeks，单双周的两条安排可能同 ID；成绩 ID 不含课程编号，考试 ID 不含学期。考试服务按 raw 去重却按另一套字段做 UI ID，保留下来的不同条目仍可能共用 ID。
- 建立稳定的领域身份，优先使用上游记录 ID；缺失时按规范化业务字段生成，并为确实不同的安排保留区分字段。不要每次刷新随机生成 UUID。测试重复显示字段但不同业务记录的输入。

### 13. 排名失败显示空白分区

- 位置：`test/Academic/LiveAcademicService.swift:247`；`test/UI/GradesView.swift:82`。
- fetchRanking 失败返回只有 title 的非 nil GradeRanking，页面照常创建 Section，RankingSummary 没有内容；同时阻止真正的空成绩分支。
- 排名增加独立加载结果；不可用时显示原因/重试，真正不存在则不创建分区。筛选学期时说明排名的统计范围，避免被误认为当前筛选学期排名。

## 需模拟器截图或真机验证的布局风险

1. 课表宽度固定 678pt，星期表头和节次随二维滚动一起离开视野。建议冻结表头/节次，手机支持日视图与周视图，宽屏按可用宽度分配列宽。
2. 单节课程卡只有 50pt 高、84pt 宽，却包含课程名两行、地点两行和周次；内容无法可靠容纳。建议卡片仅显示课程简称与地点，完整信息点击展开，大字体模式切换列表或增加行高。
3. 登录卡双层水平 padding 后，验证码行还固定占用图片 116pt、按钮 30pt、间距 20pt，小屏输入空间很窄；验证码图片 scaledToFill 后裁剪，原始宽高比不同时可能裁掉字符。改为自适应换行和 scaledToFit。
4. 当前学期和成绩筛选仅藏在图标菜单中；考试页受全局学期影响却不显示该条件。建议页面标题下显示学期与筛选标签，提供清除筛选。
5. 刷新与周次箭头补明确的无障碍标签；检查大字体、深色模式下静态品牌色和降低到 0.45 的课程透明度。

## 分阶段实施

### 第一阶段：确保数据正确

先实现 sessionGeneration 和请求身份校验；修复课表切学期；统一教学日历；考试不静默漏显；部分来源失败显式化；统一业务 ID。不要直接把初始三个查询改成无约束并发：教务系统有共享的服务器学期状态，须先保证认证 single-flight 和切学期事务边界。

### 第二阶段：统一页面状态与布局

引入带数据、更新时间、来源完整度的 ResourceState；落实强制刷新与局部重试；课表增加冲突和待定展示、详情与自适应布局；统一筛选栏、空态和登录生命周期。

### 第三阶段：降低维护与性能成本

把 714 行 AppStore 拆成认证、学期、成绩、考试、课表状态模块，由薄协调层管理会话；把 1137 行 SicauSession 的认证响应解析与网络会话分离。预解析日期/周次，避免每次视图更新重复构建正则和 DateFormatter；性能优化先测量。SwiftSoup 当前未记录精确上游 commit，应补可追溯版本与升级差异；补 CI 和可注入演示数据入口，便于无账号的 UI 测试。

## 验收矩阵

| 维度 | 必测场景 |
| --- | --- |
| 并发 | A→B→C 快速切学期、刷新中登出换号、迟到错误、重复点击 |
| 数据 | 空数据、部分来源失败、异常 HTML、未知学期、待定时间、重叠课程、重复 ID |
| 时间 | 非周一开学、单双周、学期前后、跨午夜、上海以外时区、非法日期 |
| 页面 | 小屏与大屏 iPhone、iPad、横屏、最大辅助字体、深浅色、VoiceOver、键盘弹出 |
| 网络 | 冷启动离线、加载超时、缓存刷新、取消、恢复前台、认证过期 |

现有测试覆盖常规解析、网络、存储和部分 AppStore 状态，缺少上述请求交错和 UI 布局回归。测试结果另见文末记录。

## 本次验证结果

使用 `/Applications/Xcode.app/Contents/Developer` 的完整工具链，在 iPhone 17 Pro / iOS 26.5 模拟器运行 `xcodebuild test`，退出码 0，结果为 `TEST SUCCEEDED`。独立构建目录为 `/tmp/better-sicau-review-build`，完整日志为 `/tmp/better-sicau-review-tests.log`。没有修改全局 xcode-select 配置。

构建产生两条 AppIntents 元数据提取跳过的提示，原因是 target 不依赖 AppIntents.framework。本次没有实际运行界面截图验收或学校在线接口验收，因此不将布局风险描述为已在真机复现。


## 2026-09-11 实施记录

已实施上文 1–13 项对应的主要修复：查询取消和会话代际隔离、学期确认与请求串行、日期对齐、考试未知学期保留、重叠/待定课程展示、强制刷新、各资源独立的状态/错误/更新时间、验证码输入版本保护、认证流程收尾、凭据内存清理、短信截止时间、日期严格校验与业务时钟更新、记录身份区分、排名失败提示。

新增真实查询步骤回调：成绩六步、考试四步、课表五步。服务层在执行网络/解析阶段时报告进度；等待队列和备用入口会显示具体原因。页面按步骤标出已完成、当前及待执行任务。进度条表示已完成的阶段数，不代表网络传输百分比。

实现说明：考试任一来源失败时保留上次完整结果并提示重试；首次部分成功不会被当作完整成功缓存。教学日期规则明确为“设置日期当天或之后的第一个周一”。无效/未知页面不再返回伪空数据。AppStore 保留协调职责，新增通用资源状态和独立课表排布模块；大型状态模块拆分、CI 接入及第三方依赖版本追溯仍属于后续维护工作，本次未改动 vendored SwiftSoup。

自动化验证：iPhone 17 Pro / iOS 26.5，131 个测试中 129 个通过、2 个因缺少可选真实验证码样本而跳过，0 失败。新增覆盖迟到成功/错误、学期切换、强制刷新、步骤顺序、部分来源失败、缓存清理中在途操作、未知学期、严格日期、周一对齐、冲突课程、记录 ID、手工验证码保护和后台短信倒计时。

界面验证使用完全合成的数据，已检查 iPhone 17e 的完整步骤清单、课表横纵滚动定位、重叠课程详情、大字体和失败重试。学校在线接口与真机效果仍需真实账号验收。


收尾验证：Debug 和 Release（iOS Simulator）构建均成功；补查 iPad mini 自适应周课表，以及调整后的深色主题。截图输出在 `/tmp/better-sicau-ui-review/`，查询步骤预览为 `progress.png`。本次未提交或推送 Git，保留工作区原有改动。
